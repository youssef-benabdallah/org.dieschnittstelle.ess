<resources>
    <!-- this configures the data source to the h2 database for the tomee deployment -->
    <Resource id="crm_erp_DS" type="javax.sql.DataSource">
        jdbcDriver = org.h2.Driver
        jdbcUrl = jdbc:h2:tcp://localhost/~/crm_erp_db
        jtaManaged = true
        password =
        userName = sa
    </Resource>
</resources>
